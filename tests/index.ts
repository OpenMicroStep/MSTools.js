import core = require('./core.spec');core;
import color = require('./color.spec');color;
import buffer = require('./buffer.spec');buffer;
import date = require('./date.spec');date;
import naturals = require('./naturals.spec');naturals;
import mste = require('./mste.spec');mste;
